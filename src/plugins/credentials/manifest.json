{
  "name": "credential_vault",
  "description": "Encrypted credential vault for storing per-project secrets (passwords, API keys) encrypted with OPENSHRIMP_VAULT_KEY.",
  "version": "0.1.0",
  "tags": ["credentials", "auth", "secrets"],
  "input_schema": {
    "type": "object",
    "properties": {},
    "additionalProperties": true
  }
}

